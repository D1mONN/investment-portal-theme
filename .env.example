# FILE: .env.example
# Скопіюйте цей файл як .env та заповніть значеннями

# Порт, на якому буде доступний сайт (http://localhost:8000)
HTTP_PORT=8000

# Налаштування бази даних
DB_NAME=slavuta_invest_db
DB_USER=wp_user
DB_PASSWORD=your_strong_password_here
DB_ROOT_PASSWORD=your_root_password_here

# WordPress налаштування (додайте в wp-config.php)
# define('WP_DEBUG', true);
# define('WP_DEBUG_LOG', true);
# define('WP_DEBUG_DISPLAY', false);

# Vite налаштування (додайте в wp-config.php для локальної розробки)
# define('VITE_DEV_MODE', true);
# define('VITE_HOST', 'http://localhost');
# define('VITE_PORT', 5173);

# Google Maps API (додайте в wp-config.php)
# define('GOOGLE_MAPS_API_KEY', 'your_google_maps_api_key_here');

# SMTP налаштування для відправки email (опціонально)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM=noreply@slavuta-rada.gov.ua
# SMTP_FROM_NAME=Інвестиційний портал Славути